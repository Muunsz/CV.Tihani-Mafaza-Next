// Prisma schema file

// This is where you define your data model

model User {
  id    Int     @id @default(autoincrement())
  name  String  
  email String  @unique
}


datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
  adapter = "postgresql"
}

// Add more models as needed
